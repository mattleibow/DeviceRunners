# The tools/ directory contains published MTP host tool binaries that are
# regenerated by `dotnet pack` (via the PublishMtpTool target). They should
# not be checked in — CI and local builds populate them on demand.
tools/
