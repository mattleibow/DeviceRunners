name: Wait for iOS Simulator
description: Wait for iOS simulator boot to complete and verify services are ready

inputs:
  simulator-udid:
    description: 'UDID from setup-ios-simulator output'
    required: true
  timeout:
    description: 'Max seconds to wait for boot'
    required: false
    default: '300'

runs:
  using: composite
  steps:
    - name: Wait for boot
      shell: bash
      run: |
        UDID="${{ inputs.simulator-udid }}"
        TIMEOUT=${{ inputs.timeout }}
        LOG_PATH="${{ runner.temp }}/simulator.log"

        echo "Waiting for simulator $UDID to boot (timeout: ${TIMEOUT}s)..."

        # Portable timeout: macOS lacks GNU timeout
        xcrun simctl bootstatus "$UDID" -b &
        BOOT_PID=$!
        ( sleep "$TIMEOUT" && kill "$BOOT_PID" 2>/dev/null ) &
        WATCHDOG=$!

        if wait "$BOOT_PID" 2>/dev/null; then
          kill "$WATCHDOG" 2>/dev/null || true
          wait "$WATCHDOG" 2>/dev/null || true
          echo "Simulator booted ✓"
        else
          kill "$WATCHDOG" 2>/dev/null || true
          wait "$WATCHDOG" 2>/dev/null || true
          echo ""
          echo "========================================="
          echo "TIMEOUT: Simulator did not boot in ${TIMEOUT}s"
          echo "========================================="
          echo ""
          echo "=== Simulator Devices ==="
          xcrun simctl list devices
          echo ""
          echo "=== Runtimes ==="
          xcrun simctl list runtimes
          echo ""
          echo "=== Last 50 lines of simulator log ==="
          [ -f "$LOG_PATH" ] && tail -50 "$LOG_PATH"
          exit 1
        fi

    - name: Verify SpringBoard ready
      shell: bash
      run: |
        UDID="${{ inputs.simulator-udid }}"
        LOG_PATH="${{ runner.temp }}/simulator.log"

        echo "Waiting for SpringBoard and services to be ready..."

        TIMEOUT=120
        ELAPSED=0
        while [ $ELAPSED -lt $TIMEOUT ]; do
          if xcrun simctl spawn "$UDID" launchctl print system 2>/dev/null | grep -q "com.apple.SpringBoard"; then
            echo "SpringBoard is running ✓"
            # Give services a moment to fully initialize
            echo "Waiting 10s for services to stabilize..."
            sleep 10
            break
          fi
          sleep 5
          ELAPSED=$((ELAPSED + 5))
          echo "  ... waiting for services (${ELAPSED}s / ${TIMEOUT}s)"
        done

        if [ $ELAPSED -ge $TIMEOUT ]; then
          echo "WARNING: SpringBoard readiness check timed out (services may still be starting)"
          echo "Waiting additional 15s before proceeding..."
          sleep 15
        fi

        echo ""
        echo "=== Recent simulator log ==="
        [ -f "$LOG_PATH" ] && tail -20 "$LOG_PATH"
        echo ""
        echo "Simulator $UDID is ready ✓"
