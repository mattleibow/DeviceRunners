name: 'TCP Windows Test (Unpackaged)'
description: 'Run TCP-based device tests on Windows (unpackaged executable)'

inputs:
  test-project:
    description: 'Path to the test project'
    default: 'sample/test/DeviceTestingKitApp.DeviceTests/DeviceTestingKitApp.DeviceTests.csproj'
  target-framework:
    description: 'Target framework'
    default: 'net10.0-windows10.0.19041.0'
  configuration:
    description: 'Build configuration'
    default: 'release'
  artifact-name:
    description: 'Artifact name'
    default: 'Test Results (TCP) - Windows Unpackaged'

runs:
  using: 'composite'
  steps:
    - name: Setup Required Tools
      uses: ./.github/workflows/setup-tools
    - name: Build DeviceRunners.Testing.Targets Package
      shell: pwsh
      run: |
        dotnet pack src/DeviceRunners.Testing.Targets/DeviceRunners.Testing.Targets.csproj -c release
    - name: Run Tests
      shell: pwsh
      run: |
        dotnet test ${{ inputs.test-project }} `
          -f ${{ inputs.target-framework }} `
          -c ${{ inputs.configuration }} `
          /bl:./artifacts/logs/msbuild-test.binlog
    - name: Upload Artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.artifact-name }}
        path: ./artifacts
